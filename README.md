A chatbot (text+image)<br>
After cloning the repo :
setup : 
- python -m venv venv
- source venv/bin/activate

Install dependencies :
- pip install -r requirements.txt

Run :
- Paste the api key in your .env file in GEMINI_API_KEY  variable
- run python app.py command to the run the backend
- 
